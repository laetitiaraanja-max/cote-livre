# --- Cles API (toutes cote serveur, jamais exposees au navigateur) ---

# Requis pour l'extraction IA (lecture de la couverture par Claude vision).
ANTHROPIC_API_KEY=

# Scraping AbeBooks via Apify. Laisser VIDE pour la demo : le systeme bascule
# automatiquement sur le cache de demo. A renseigner apres signature.
APIFY_TOKEN=

# Optionnel : enrichissement metadonnees des livres modernes par ISBN.
GOOGLE_BOOKS_API_KEY=

# Optionnel : persistance des fiches. Sans Supabase, rien n'est enregistre
# (cela ne bloque pas la demo).
SUPABASE_URL=
SUPABASE_ANON_KEY=
